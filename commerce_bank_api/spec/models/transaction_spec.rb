require 'rails_helper'

RSpec.describe AccountTransaction, type: :model do
  #subject {AccountTransaction.new("DR",1000)}
  #it(:transaction_type) {should == "DR"}
  #it(:amount) {should == 1000}
  # #Assosiciation test
  # it {should have_many(:triggered_events)}
  #
  # #Validation tests
  # it {should validate_presence_of (:transaction_type)}
  # it {should validate_presence_of (:amount)}

end
